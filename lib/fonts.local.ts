/**
 * Local fallback fonts for CI environments without network access
 */
export const inter = {
  className: 'font-sans',
  style: {
    fontFamily:
      'system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif',
  },
};
