# Ensure consistent lockfile across npm versions.
# CI uses Node 20.x (npm ~10.x); local dev may use newer versions.
# save-exact prevents unintended caret/tilde ranges when adding deps.
save-exact=true
